# CNG / Jamesons Context

The project is a fresh standalone PHP 8 + MySQL ticketing system at `C:\xampp\htdocs\cng-ticketing`. It runs locally under XAMPP and will deploy to cPanel. No Composer or framework is allowed. SMTP configuration is outside Git. The Jamesons logo will later be dropped into `assets/`.

